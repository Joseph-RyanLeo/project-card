# Project Card Agents Guide

本文件补充上层 `/Users/Zhuanz/Documents/Codex/AGENTS.md`。
如有冲突，以本文件为准。

## 项目定位

Project Card 是一个像素风格的卡牌自走棋游戏，计划使用 Godot 开发。

游戏强调轻量操作、非即时反应压力，以及围绕卡牌、阵容、堆叠、元素符文、牌型和自动战斗形成的策略体验。目标用户是喜欢像素风格、卡牌构筑和自走棋玩法的玩家。

项目名称暂定为 Project Card，后续可能调整。

## 当前阶段

项目处于准备开始编写代码的阶段。

当前开发者正在学习 GDScript，对 Godot 的类、节点、信号等概念已有初步了解。后续开发应兼顾实现和教学：不仅完成代码，也要解释关键 Godot / GDScript 设计思路，帮助开发者逐步掌握项目。

## 技术方向

- 使用 Godot 制作，当前计划版本为 Godot 4.7。
- 主要脚本语言预计为 GDScript。
- 开发时优先采用 Godot 原生节点、资源、信号和场景组织方式。
- 在引入第三方插件、外部工具或大型依赖前，必须先说明原因并获得确认。
- 由于 Godot 项目版本升级可能影响场景、资源和脚本行为，升级 Godot 版本前必须先说明风险并获得确认。

## 体验参考

- 游戏视角和主要界面观感可参考《Balatro / 小丑牌》一类 2D 卡牌桌面视角。
- 项目美术方向为像素风。
- 卡牌信息需要支持鼠标悬停、右键切换展示、图标化数值和紧凑布局。

## 协作方式

- 默认使用中文沟通。
- 默认只在 `/Users/Zhuanz/Documents/Codex/Project Card` 主项目目录和 `main` 分支中工作，确保项目文件只有一个明确的当前版本。
- 除非用户明确要求，不得创建新的 Git worktree、`agent/*` 临时分支或其他字母数字命名的并行开发区；多个开发任务按顺序完成。
- 开始修改前检查当前目录与分支，玩法设计以主项目目录中的 `GAMEPLAY_DESIGN.md` 为唯一权威本地版本。
- 修改前先阅读相关文件和已有项目约定。
- 通过 Codex 或终端执行 Godot 自动测试、导入或无窗口场景加载时，每个进程必须显式使用 `--log-file /private/tmp/project-card-<用途>.log`，并串行执行，不得并行启动多个 Godot 实例；这可避免受限环境无法写入默认 `user://logs` 或争用日志轮转时触发 Godot 4.7.1 原生崩溃。
- 对初学者不友好的 Godot / GDScript 写法，需要附带简短解释。
- 人工可调的尺寸、间距、透明度、速度、角度和动画时长等参数，声明时应使用同排中文 `#` 注释说明它控制的效果；普通运行状态和含义明显的局部变量不强制逐个注释。
- 不只给结果，也要说明关键代码为什么这样组织。
- 功能复杂时，先拆成小步实现，避免一次性生成过大的系统。
- 修改现有功能时优先复用、修改或删除已有实现，不要为了绕开现有结构而一味叠加新变量、重复函数、兼容层或额外分支；确需新增时，应说明现有代码为何无法清晰承载，并同步清理已经失效的实现与测试。
- 项目设定、卡牌设计、玩法数值会逐步补充，不要擅自定死核心规则。
- 遇到规则表述不明确、实现可能有多种解释、或数值会影响核心玩法时，直接向用户提出问题。

## 阶段教学与验收

每次完成一个开发阶段后，必须面向正在学习 Godot / GDScript 的开发者，按以下三层结构说明本阶段实现：

1. 先说明功能整体如何运作，以及输入到结果的主要流程。
2. 再说明最关键的数据、变量、场景、脚本和函数各自负责什么。
3. 最后单独解释对初学者不友好的 Godot / GDScript 写法及采用原因。

说明应帮助开发者逐步接管代码，不要求逐行讲解普通样板代码，也不能只汇报修改结果而省略实现逻辑。

当用户表示准备结束当前阶段或进入下一阶段时，必须先进行一次阶段学习验收，不要直接开始下一阶段。验收问题应结合刚完成的实际功能，确认开发者能够：

1. 说出本阶段由哪些主要场景和脚本负责。
2. 用自己的话复述功能从输入到结果的主要流程。
3. 指出核心数据保存在哪些变量或资源中。
4. 亲自完成一个小行为或数值修改；由 Codex 给出明确且低风险的练习目标，在开发者卡住时提供提示。
5. 说明并实际执行一种验证方式，确认修改没有破坏已有功能。

Codex 需要检查回答和练习结果；若仍有关键理解缺口，应先针对缺口重新解释或提供提示，再继续验收。完成上述验收后，才能进入下一开发阶段。该验收只在阶段交接时触发，不应打断当前阶段内部的正常实现和调试。

## 设计资料

详细玩法、卡牌类型、堆叠规则、牌型、行动方式、准备与战斗流程记录在：

- `GAMEPLAY_DESIGN.md`
- `docs/VISUAL_REFERENCES.md`
- `notes/chat-summary-2026-07-05.md`
- `notes/open-questions.md`

部分卡牌设计、玩法数值和规则资料可能存放在飞书表格中。

在未接入飞书读取能力前，优先通过以下方式同步资料：

- 用户手动复制关键表格内容。
- 导出为 `.xlsx` / `.csv` 后放入项目目录。
- 后续如接入飞书 API，应只读取用户明确授权的表格和字段。

## 待补充

- 最终项目名称。
- Godot 项目目录结构。
- 核心数据结构。
- 初始英雄、阵营和卡包设定。
- 卡牌字段表。
- 棋盘坐标和卡牌堆叠实现方案。
- 战斗时间轴和结算顺序。
- 美术资源命名规则。
- 飞书表格同步方式。
