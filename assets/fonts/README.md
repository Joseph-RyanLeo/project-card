# 正式卡牌字体

卡名与效果文字固定使用 `chill_7.ttf`（寒蝉点阵 7px），Godot 字号为 8。
行动值与生命值使用 `pixel_numbers_large.fnt` 的 12px 数字字模；护甲恢复使用对称的 `pixel_numbers_small.fnt` 8px 数字字模。

寒蝉点阵原始文件仍保存在用户提供的美术素材目录中；项目内已删除字体选型阶段的其余候选副本。
